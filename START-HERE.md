# منصة السعيد V15 — ابدأ من هنا

هذه نسخة تشغيل كاملة لمنصة السعيد: واجهة HTML، خادم Express، قاعدة SQLite، إدارة المستخدمين والباقات والمحتوى والدفع والشهادات.

## التشغيل المحلي

```bash
cp .env.example .env
npm install
npm start
```

ثم افتح `http://localhost:3000`.

## حساب المشرف

لا توجد بيانات دخول افتراضية. عند أول تشغيل أنشئ المشرف من ملف `.env`:

```env
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-password
JWT_SECRET=replace-with-a-long-random-secret
```

كلمة مرور المشرف يجب أن تكون 10 أحرف على الأقل.

## ما يديره المشرف

- الدورات والباقات: إضافة وتعديل وحذف، السعر، اللغة، المدة، الساعات، المحتوى والشهادة.
- الأنظمة والأدوات المستقلة: السعر، اللغة، المزايا، مدة الوصول والرابط.
- أكواد الخصم: النسبة وتاريخ الانتهاء؛ يتحقق منها الخادم قبل إنشاء عملية الدفع.
- فيديوهات Vimeo والدروس والاختبارات القصيرة.
- المستخدمون والاشتراكات والطلبات والتقدم.

## ملاحظات النشر

- اضبط بوابة الدفع المناسبة ومتغيراتها في `.env`.
- استخدم تخزينًا دائمًا لملف SQLite واضبط النسخ الاحتياطي.
- اختبر الدفع وWebhook في بيئة الاختبار قبل الإنتاج.
- الصفحات الكاملة للاختبارات والأدلة تتحقق من اشتراك المستخدم عبر `public/assets/lms.js`.
