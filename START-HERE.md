# AL-LTC V8 — ابدأ من هنا

هذه النسخة مبنية مباشرة على ملف **AlSaeed-Platform-Full (3).zip** المرفق من المستخدم، وليست نسخة سابقة.

## ما تغير بوضوح
- Hero جديد غني بصرياً ومهيأ لفيديو Vimeo.
- بطاقات برامج جديدة بها غلاف بصري ومعاينة سريعة.
- صفحة الدورة أصبحت Landing Page كاملة: غلاف، فيديو Preview، KPIs، طرق التدريب، محاور وفصول وباقات.
- القائمة العلوية ثابتة وتترجم فعلياً للعربية/الإنجليزية/الفرنسية/التركية/الأردية.
- شعار جديد AL-LTC يجمع التعليم والنمو.
- لوحة Admin جديدة لمتابعة الطلاب والتقدم والاختبارات وآخر نشاط والانتهاء.
- بيانات Admin أصبحت تُسحب من الخادم فعلياً عبر `/api/admin/overview`.
- شاشة متابعة فردية لكل طالب + تصدير CSV.
- إضافة Cover URL وVimeo Preview لكل دورة من لوحة الإدارة.
- إضافة Vimeo Hero من CMS دون تعديل الكود.

## التشغيل
```bash
cd server
cp .env.example .env
# عدّل ADMIN_EMAIL وADMIN_PASSWORD وJWT_SECRET وباقي القيم
npm install
npm start
```
ثم افتح:
`http://localhost:3000`

## حساب المشرف
لا توجد كلمة مرور Admin افتراضية داخل الكود. في أول تشغيل فقط، إذا لم يكن هناك مشرف في قاعدة البيانات، يستخدم الخادم:

```env
ADMIN_EMAIL=...
ADMIN_PASSWORD=... # 10 أحرف على الأقل
```

## إضافة فيديو للواجهة الرئيسية
Admin → تحرير الموقع → **Vimeo ID للفيديو التعريفي**.

مثال: لو رابط Vimeo هو:
`https://vimeo.com/123456789`
اكتب فقط:
`123456789`

## إضافة صورة وفيديو لدورة
Admin → الدورات والباقات → تعديل دورة:
- صورة غلاف الدورة: URL لصورة.
- Vimeo Preview ID: رقم فيديو تعريفي قصير.

إن لم تضف صورة، يولد التصميم غلافاً بصرياً تلقائياً حسب مجال الدورة.

## لوحة الإدارة
بعد الدخول كمشرف ستجد:
- نظرة عامة
- المسجلون
- الطلبات
- تحرير الموقع
- الدورات والباقات
- Vimeo والمحتوى
- الدفع

تبويب **المسجلون** يعرض:
- عدد الاشتراكات
- نسبة التقدم
- متوسط الاختبارات
- آخر نشاط
- أقرب تاريخ انتهاء
- زر متابعة يفتح تفاصيل الطالب


## تحديث V9

تمت إضافة صور Hero وأغلفة جاهزة داخل مجلد assets، وهي تظهر مباشرة افتراضياً في الموقع حتى بدون إدخال روابط خارجية.
